import React from 'react'

const Cart = () => {
    //useState change if theres added to cart?
    //needs to be currentUser to have access
    return (
        <div>
            Cart
        </div>
    )
}

export default Cart